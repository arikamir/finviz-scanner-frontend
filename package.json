{
  "name": "finviz-scanner-frontend",
  "version": "1.0.1",
  "description": "Frontend web interface for Finviz ATR Swing Scanner with Gmail authentication",
  "main": "frontend.html",
  "scripts": {
    "lint:html": "htmlhint --config .htmlhintrc *.html",
    "lint:css": "stylelint **/*.css",
    "lint:js": "eslint **/*.js",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js",
    "docker:build": "docker build --target production -t finviz-frontend .",
    "docker:build:dev": "docker build --target development -t finviz-frontend:dev .",
    "docker:build:prod": "docker build --target production -t finviz-frontend:prod .",
    "docker:build:test": "docker build --target test -t finviz-frontend:test .",
    "docker:run": "docker run -p 80:80 finviz-frontend",
    "docker:run:dev": "docker run -p 8080:80 finviz-frontend:dev",
    "compose:up": "docker-compose up -d",
    "compose:down": "docker-compose down",
    "compose:build": "docker-compose up --build -d",
    "test": "echo \"Frontend testing via Docker containers in CI\"",
    "start": "echo \"Open frontend.html in your browser or use Docker\"",
    "validate": "npm run lint",
    "version:current": "node scripts/version.js current",
    "version:patch": "node scripts/version.js bump patch",
    "version:minor": "node scripts/version.js bump minor",
    "version:major": "node scripts/version.js bump major",
    "version:tag": "node scripts/version.js tag",
    "version:release": "node scripts/version.js release"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arikamir/finviz-scanner-frontend.git"
  },
  "keywords": [
    "finviz",
    "scanner",
    "trading",
    "stocks",
    "atr",
    "swing-trading",
    "frontend",
    "web-app",
    "gmail-auth"
  ],
  "author": "arikamir",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/arikamir/finviz-scanner-frontend/issues"
  },
  "homepage": "https://github.com/arikamir/finviz-scanner-frontend#readme",
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "stylelint": "^15.10.3",
    "stylelint-config-standard": "^34.0.0",
    "eslint": "^8.48.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
